<template>
  <button class="btn" t-on:click="bump">独立组件状态：{{ n }}</button>
</template>

<script>
import { setup } from "tee";

export default setup({
  tag: "mini-counter",
  setup(c) {
    c.n = 0;
    c.bump = () => {
      c.n = Number(c.n) + 1;
    };
  },
});
</script>
