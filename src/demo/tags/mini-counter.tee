<template>
  <button class="btn" t-on:click="bump">独立组件状态：{{ n }}</button>
</template>

<script>
import { weave } from "tee";

export default weave({
  tag: "mini-counter",
  install(c) {
    c.hold({ n: 0 });
    c.act("bump", () => {
      c.n = Number(c.n) + 1;
    });
  },
});
</script>
